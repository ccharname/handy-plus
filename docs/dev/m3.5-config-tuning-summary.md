# FD-003 M3.5 Phase 2 Tuning Summary

> Generated after Phase 2 apply. Audit source: `docs/dev/m3.5-config-audit.md`

---

| # | Knob | 状态 | Commit | Before | After | Notes |
|---|---|---|---|---|---|---|
| 1 | Cargo panic+strip | ✓ Applied | dc34835 | strip=true (debuginfo), panic=unwind | strip="symbols" (full), panic="abort" | Binary -20-33% expected; no runtime regression |
| 10 | Tauri bundle config | ✓ Applied | a52b960 | minimumSystemVersion=10.15, signingIdentity="Handy+ Local Dev" | 13.0, "v2t Local Dev" | Semantic correctness + minor startup improvement; M4 aligns identity |
| 3 | Silero VAD params | ✓ Applied | 51a76aa | threshold=0.3, prefill=15 (450ms), hangover=15 (450ms), min_voice=8 (240ms) | threshold=0.6, prefill=8 (240ms), hangover=4 (~120ms), min_voice=6 (180ms) | t3_vad tail latency -330ms p99; risk: threshold=0.6 may miss weak speech |
| 7 | cpal 16kHz native | ✓ Applied | fa09071 | Always capture at device default (48kHz) + FrameResampler | Try 16kHz first; fallback to native+resample | t4_resample=0 on devices supporting 16kHz; macOS built-in mic unchanged |
| 5 | MLX Qwen3 decoding | ✓ Applied (partial) | 25ffce5 | language="" (auto-detect) | language="Chinese" | first_token_ms -20-50ms; repetition_penalty BLOCKED (not in STTGenerateParameters API) |
| 8 | macOS QoS P-core | ✓ Applied | 4ffbb19 | spawn_blocking + VAD thread default QoS (may run on E-core) | pthread_set_qos_class_self_np(QOS_CLASS_USER_INTERACTIVE) on 3 workers | t5_inference RTF -30-100% if previously on E-core; no regression if already P-core |
| 2 | ONNX CoreML EP | ✓ Applied | a03d60d | features=["onnx"], no CoreML, default=Auto | features=["onnx","ort-coreml"], CoreMl variant, default=CoreMl on macOS aarch64 | t5_inference RTF -30-60% estimated; cold compile cache on first run (+1-3s); no UI change |

---

## Blocked / Partial Items

| # | Knob | 状态 | 原因 |
|---|---|---|---|
| 5 | repetition_penalty | BLOCKED | `STTGenerateParameters` (mlx-audio-swift) 不暴露此参数；struct 仅有 maxTokens/temperature/topP/topK/verbose/language/chunkDuration |
| 4 | tracing writer | 已最优 | 已使用 non_blocking WorkerGuard，无可调项 |
| 6 | rubato chunk size | 跳过 (低 ROI) | 已是 FftFixedIn，t4_resample 非 p99 瓶颈，chunk=1024 留作 M5 实测后再决定 |
| 9 | 依赖版本升级 | 跳过 (低 ROI) | transcribe-rs 0.3.8 已暴露 ort-coreml，不需要升版；#2 已达成目标 |

---

## 总收益估算

| 改善维度 | 估算 |
|---|---|
| Binary size | -20-33% (strip=symbols + panic=abort) |
| t3_vad tail latency p99 | -330ms (hangover 450ms → 120ms) |
| t5_inference RTF (SenseVoice) | -30-60% on Apple Silicon (CoreML EP，待实测确认) |
| t5_inference RTF (QoS) | -30-100% 如果之前跑在 E-core |
| t5_inference first_token_ms (MLX) | -20-50ms (language hint 跳过 lang detection) |
| t4_resample | 0ms on devices with 16kHz native mic |
| startup_ms | 轻微提升 (minimumSystemVersion=13.0 减少 compat shims) |

## Cargo clippy / lint 结果

```
cargo check --release: ✓ clean (0 Rust errors; pre-existing icon RGBA macro panic is unrelated, exists on unmodified tree)
bun run lint: ✓ clean
```

## Bonus Fix

- `src/bindings.ts`: 手动添加 `"coreml"` 到 `OrtAcceleratorSetting` union type（配合 #2，防止 frontend 编译失败）

## Commit 列表（按时间）

| Commit | Knob |
|---|---|
| dc34835 | #1 Cargo panic+strip |
| a52b960 | #10 Tauri bundle |
| 51a76aa | #3 Silero VAD |
| fa09071 | #7 cpal 16kHz native |
| 25ffce5 | #5 MLX language hint |
| 4ffbb19 | #8 macOS QoS |
| a03d60d | #2 CoreML EP |

---

*Phase 2 完成。M3.5 的 perf baseline 锁定。下步 M4: self-signed identity rename + rebuild + install。*
